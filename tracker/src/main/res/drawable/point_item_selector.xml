<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Палец нажимает на пункт, но пункт ещё не активирован
    <item android:state_pressed="true"> <ripple android:color="@color/colorSwipeMenuPreselectMode"/> </item> -->
    <!--<item android:state_pressed="true" android:drawable="@color/colorSwipeMenuPreselectMode" /> -->
    <item android:state_pressed="true"> <ripple android:color="@color/colorSwipeMenuPreselectMode"/> </item>

    <!-- Пункт активирован. В режиме SINGLE_CHOICE_MODE пункт подсвечивается -->
    <item android:state_activated="true" android:drawable="@color/colorSwipeMenuEditMode" />

    <!-- По умолчанию -->
    <item android:drawable="@android:color/transparent" />
</selector>